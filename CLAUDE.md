# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository purpose

Advanced Process Mining (APM) assignment project. **Part 1 (Q3) is complete.** Active work is **Part 2: Q3, Q5a, Q5b** — Python verification code and LaTeX report sections. Instead of OCPQ query screenshots, produce Python code that verifies the same results.

Assignment PDFs are in `doc/`. Part 2 spec: `doc/apm26-assignment-part-2.pdf`.

---

## Python environment

**All Python commands must be run from the `code/` directory** (the Poetry project root).

```bash
cd code
poetry install          # first-time setup
```

System requirement: **Graphviz** (`dot` binary) must be on `PATH`. `config.py` auto-probes common Windows install paths.

### Run the Part 1 pipeline (in order)

```bash
poetry run generate     # → data/log.sqlite + data/log.json
poetry run validate     # requirement checks R(i)–R(v)
poetry run discover     # flattened Petri nets → models/ + figures/
poetry run ocpn         # discovered OCPN N2 → figures/
poetry run dotted       # dotted chart → figures/
poetry run queries      # Python equivalents of OCPQ queries
```

### Run individual modules

```bash
poetry run python -m olympic_transport.<module_name>
```

---

## LaTeX report

Report source lives in `report/`. Build from there:

```bash
cd report
latexmk -pdf main.tex   # → main.pdf
latexmk -c              # clean aux files
```

`report/sync_figures.py` copies generated figures from `figures/` into `report/figures/`. Run it before building the report after regenerating artifacts.

Report structure mirrors `doc/DOCUMENTATION_SPECIFICATION.md`:
- `report/main.tex` — master file, `\input`s sections
- `report/preamble.tex` — packages and macros (entity-name macros like `\otPassenger`)
- `report/sections/q3_*.tex` — one file per subtask
- `report/figures/` — copies of generated figures

LaTeX conventions (from the spec):
- Use preamble macros (`\otPassenger`, `\etDepart`, …) for entity names — never hard-code them in prose.
- Cross-references always via `\cref{}`, never hard-coded numbers.
- `booktabs` rules only (no vertical lines in tables).
- `[H]` placement for figures.

---

## Code architecture

### Part 1 module layout (`code/olympic_transport/`)

| File | Role |
|---|---|
| `config.py` | **Single source of truth.** All object types, event types, attribute names, sizing constants, RNG seed, and file paths. Edit here first. |
| `generate_log.py` | Builds and exports the OCEL 2.0 log to `data/log.sqlite` using `r4pm`. |
| `validate.py` | Checks requirements R(i)–R(v) against the loaded OCEL. |
| `flatten_discover.py` | Flattens OCEL to `TravelTicket` and `Passenger`, discovers Petri nets with PM4Py inductive miner. |
| `discover_ocpn.py` | Discovers the object-centric Petri net N2. |
| `dotted_chart.py` | Renders synchronization dotted chart (R viii-a evidence). |
| `queries.py` | Python equivalents of OCPQ queries (f-i, f-ii, f-iii, g). |
| `render_tables.py` | Emits E2O/O2O tables as Markdown from `config.py` spec. |

### Data pipeline dependency order

```
config.py
    └─▶ generate_log.py ──▶ data/log.sqlite
            ├─▶ validate.py
            ├─▶ flatten_discover.py  → models/ + figures/
            ├─▶ discover_ocpn.py     → figures/
            ├─▶ dotted_chart.py      → figures/
            └─▶ queries.py
```

Nothing downstream can run before `data/log.sqlite` exists.

### Key data files

| File | Description |
|---|---|
| `data/log.sqlite` | Part 1 OCEL 2.0 log (generated by `generate_log.py`) |
| `data/Road_Traffic_Fine_Management_Process.xes.gz` | Standard PM4Py traffic fine log (for Part 2) |
| `data/version_control.sqlite` | OCEL 2.0 log (for Part 2) |

---

## Part 2 scope

**Responsible for Q3, Q5a, Q5b only.** See `doc/apm26-assignment-part-2.pdf` for exact question text.

- Q3 uses **Python code instead of hand calculation**: the GSLPN (places/transitions/weights,
  entered by hand from the figure on assignment PDF p.6) is solved generically as a product of
  the marking graph with a "how much of sigma has been matched" counter — max-product Dijkstra
  for part (a) (single highest-probability path), exact linear-system absorption probability for
  part (b) (total P(sigma), needed because the silent t9-loop admits infinitely many matching
  paths). Run `poetry run q3`.
- Q5a and Q5b use **Python code instead of OCPQ queries**. Run `poetry run q5a` and `poetry run q5b`.
- The Part 2 LaTeX report is in `report2/` (separate from Part 1's `report/`). Build with `latexmk -pdf main.tex` from `report2/`.

### Part 2 Python modules

| Script entry | Module | Purpose |
|---|---|---|
| `poetry run q3` | `q3_gslpn.py` | Solve GSLPN path (a) and trace (b) probabilities for sigma = <a,d,c,e,f> |
| `poetry run q5a` | `q5a_constraints.py` | Check C1, C2, C3 on `version_control.sqlite` |
| `poetry run q5b` | `q5b_conformance.py` | Filter to Cameron, flatten, token replay, precision |

### What still needs manual work (Part 2)

Note: Part 2's report is a single `report2/main.tex` (no separate `sections/` files
despite the module-per-question table above referencing script names).

| Item | Location | Action needed |
|---|---|---|
| Q3 (a) and (b) | `report2/main.tex` (`sec:q3a`, `sec:q3b`) | Copy `poetry run q3` output into the `\pathtrace` tables and probability values — still `\TODO` |

Q5a is done (real OCPQ screenshots + explanations already in `report2/main.tex`).
Q5b is done (N1 subnet figure, flattened traces, token-replay tables, alignments,
prefix automata figures, and C1/C2 interpretation all filled in from
`poetry run q5b` output — see `q5b_conformance.py`).

### Data for Part 2

| File | Description |
|---|---|
| `data/Road_Traffic_Fine_Management_Process.xes.gz` | Road traffic fine log (Q1b, not our scope) |
| `data/version_control.sqlite` | Git version-control OCEL 2.0 log (Q5) |

### Database schema (version_control.sqlite)

Object types: `branches` (3), `users` (4), `files` (6), `file versions` (54)  
Event types: `commit` (39), `pull` (26), `push` (30), `merge` (5)  
`event_object` qualifiers: NULL (default), `consume`, `produce`  
`object_object` qualifier: `is_a` (file version → file)

---

## Key design invariants (Part 1)

- RNG seed is fixed (`SEED = 42` in `config.py`) — regenerating the log reproduces every figure and query result exactly.
- `fare_class` on `TravelTicket` is a **static** attribute encoded as a single epoch-stamped `r4pm` history entry. Do not write multiple history values.
- `AssignToDeparture` and `Depart` are emitted **once per departure** over the **identical** passenger set — diverging these two sets silently breaks R(viii-a).
- PM4Py API names drift between versions. The installed version is pinned in `code/poetry.lock`. Check actual installed API before calling flattening/visualization helpers.
